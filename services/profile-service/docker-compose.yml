
services:
  app:
    build: .
    ports:
      - "4001:4001"
    environment:
      - DATABASE_URL=postgres://aprecot:12345@db:5432/profileservice
    depends_on:
      - db
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "npx sequelize-cli db:migrate && npm start"  # Cambiar aquí

  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: aprecot
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: profileservice
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
